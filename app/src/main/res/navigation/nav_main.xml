<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_main"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="id.buaja.post.ui.home.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_homeFragment_to_searchPostFragment"
            app:destination="@id/searchPostFragment" />
    </fragment>
    <fragment
        android:id="@+id/searchPostFragment"
        android:name="id.buaja.post.ui.search.SearchPostFragment"
        android:label="fragment_search_post"
        tools:layout="@layout/fragment_search_post" />
</navigation>